<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clientecrudproviderehvm">
    <uses-permission android:name="net.ivanvega.actividadesenandorid.WRITE_PERMISSION"/>
    <uses-permission android:name="net.ivanvega.actividadesenandorid.READ_PERMISSION"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClienteCRUDProviderEHVM">
        <activity android:name=".ui.SeleccionarUno"></activity>
        <activity android:name=".ui.Modificar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ClienteCRUDProviderEHVM.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>